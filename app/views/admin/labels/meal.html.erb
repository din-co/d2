
<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.name, admin_meal_label_print_path(product) %>
      <% if product.restaurant.present? %>
        (<%= product.restaurant.name %>)
      <% end -%>
    </li>
  <% end -%>
</ul>
